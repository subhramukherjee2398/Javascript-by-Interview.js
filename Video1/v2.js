const obj = {
    a : "one",
    b : "two",
    a : "three"
}

console.log(obj)

/*
{a : three  , b : two}

if any object have duplicate keys and their value will eb replace by the end key of those duplicated keys.
*/